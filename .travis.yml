language: python

os:
  - linux

python:
  - "3.4"
  - "3.5"
  - "3.5-dev"
  - "3.6"
  - "3.6-dev"

before_install:
  - cp README.md README.rst
  - pip install flake8

install:
  - pip install -e .

script:
  - python -c "import nanomath"
  - flake8 . --exclude=setup.py,nanomath/__init__.py
